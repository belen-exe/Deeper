lista x = [1, 2, 3, 4, 5];
lista y = [1, 3, 2, 5, 4];

entero m = regresion_pendiente(x, y);
entero b = regresion_intercepto(x, y);

mostrar("pendiente m =", m);
mostrar("intercepto b =", b);

lista pred = predecir(x, m, b);
mostrar("predicciones =", pred);
