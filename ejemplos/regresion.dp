importar StanLearn como sl;

lista x = [1, 2, 3, 4, 5];
lista y = [1, 3, 2, 5, 4];

decimal m = sl.regresion_pendiente(x, y);
decimal b = sl.regresion_intercepto(x, y);

mostrar("pendiente m =", m);
mostrar("intercepto b =", b);

lista pred = sl.predecir(x, m, b);
mostrar("predicciones =", pred);
